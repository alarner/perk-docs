* When setting up a route, describe which things need slashes in front of them and which don't (rendering views).
* howhap middleare should throw better error if you pass in a string that doesn't work